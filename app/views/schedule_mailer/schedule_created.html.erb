<p><%= @user.full_name %>,</p>

<p><%= @schedule_noun %> been created for you. You can go to <%= link_to "the schedule site", root_url(host: JobScheduler::Application.config.url_host) %> to view your current schedule.</p>

<p>
  <strong>Job name: </strong><%= "#{@job.customer.try(:name)}: #{@job.name}" %><br>
  <strong>Scheduled Times:</strong><br>
  <ul>
    <% @schedule_blocks.each do |schedule_block| %>
      <ul><%= schedule_block[:from_time].strftime("%m/%d/%Y %I:%M%P") %> - <%= schedule_block[:to_time].strftime("%m/%d/%Y %I:%M%P") %></ul>
    <% end %>
  </ul>
</p>